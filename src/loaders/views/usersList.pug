extends layout

block content
    p User: #{us.login}

    table(border='1px')
        th UserName
        th Roles
        th FullName
        for user in users
            tr 
                td #{user.login}
                td
                    for role in user.roles
                        span #{role.name}, 
                td #{user.user_info.full_name}