extends layout

block content
    div(class='container')
        table(class='table')
            th UserName
            th Roles
            th FullName
            for user in users
                tr 
                    td #{user.login}
                    td
                        for role in user.roles
                            span #{role.name}, 
                    td #{user.user_info.full_name}